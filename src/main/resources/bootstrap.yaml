spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-stitching
      aliases:
        microservicekey-em-stitching-api: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        docmosis-access-key: DOCMOSIS_ACCESS_KEY
