java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  readinessDelay: 45
  environment:
    RUN_DB_MIGRATION_ON_STARTUP: 'false'
    TASK_ENV: documentTaskLock-demo-staging
